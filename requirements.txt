# Run the notebooks: reproduce every figure/table and load the datasets.
# No GPU or API keys needed for this path. (Pinned to the versions the notebooks
# were validated against.)
numpy==2.2.6
pandas==2.3.2
scipy==1.15.3
matplotlib==3.10.3
seaborn==0.13.2
soundfile==0.13.1
librosa==0.11.0
datasets==3.6.0
huggingface_hub==1.0.1
python-dotenv==1.1.1
ipython==8.12.3
ipywidgets==8.1.8
jupyterlab>=4.0

# To RE-RUN the audio pipeline / model evaluations (XLS-R, Whisper, the model
# clients, ElevenLabs TTS), also install the heavier extras:
#     pip install -r requirements-pipeline.txt
